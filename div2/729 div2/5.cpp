#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

const int N = 3e5 + 10;

int t[N], d[N];

int main(void) {
    int q;
    cin >> q;
    while (q --) {
        int n, k;
        cin >> n >> k;
    }

    return 0;
}