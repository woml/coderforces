#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

const int N = 1e5 + 10;
int n, m;

void solve() {
    string s;
    cin >> n >> m;
    cin >> s;
    while (m -- ) {
        int x;
        cin >> x;
        if (x % 2 == 0) cout << "NO\n";
        else {
            
        }
    }
}

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int cases;
    // cin >> cases;
    // while (cases -- ) {
        solve();
    // }

    return 0;
}
